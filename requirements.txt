# Core. Everything the engine imports.
numpy>=1.24
pandas>=2.0
scipy>=1.10

# Reporting and charts.
matplotlib>=3.7

# Tests.
pytest>=7.4

# Optional: only needed by quant-portfolio/, which downloads from
# Yahoo Finance. The risk engine itself reads local HistData files
# and makes no network calls.
yfinance>=0.2.40
